---
import SEO from "./SEO.astro";
import { MAIN_SITE_URL } from "../lib/movies.js";
import "../styles/global.css";

const { title, description, image, path, type, keywords, jsonLd, noindex } = Astro.props;
---

<!doctype html>
<html lang="he" dir="rtl">
<head>
  <SEO
    title={title}
    description={description}
    image={image}
    path={path}
    type={type}
    keywords={keywords}
    jsonLd={jsonLd}
    noindex={noindex}
  />
</head>
<body>
  <header class="site-header">
    <div class="container bar">
      <a href="/" class="brand">ZOVEX</a>
    </div>
  </header>

  <main class="container">
    <slot />
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>ZOVEX &mdash; צפייה ישירה בסרטים, סדרות ואנימה. <a href={MAIN_SITE_URL}>לצפייה עכשיו</a>.</p>
    </div>
  </footer>
</body>
</html>
